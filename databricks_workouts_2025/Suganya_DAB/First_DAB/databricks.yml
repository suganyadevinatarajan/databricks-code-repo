# This is a Databricks asset bundle definition for First_DAB.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: First_DAB
  uuid: b0b93ae5-bf89-40df-9e48-974c045f6b02

include:
  - resources/*.yml
  - resources/*/*.yml

artifacts:
  python_artifact:
    type: whl
    build: uv build --wheel

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://dbc-e15125f8-8eb5.cloud.databricks.com
    variables:
      catalog: catalog1_we4
      schema: schema1_we4
  prod:
    mode: production
    workspace:
      host: https://dbc-e15125f8-8eb5.cloud.databricks.com
      # We explicitly deploy to /Workspace/Users/suganyadevi2803@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/suganyadevi2803@gmail.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: prodcatalog1
      schema: logistics1
    permissions:
      - user_name: suganyadevi2803@gmail.com
        level: CAN_MANAGE
